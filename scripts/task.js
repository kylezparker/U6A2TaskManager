class Task {
  // title,duration,deadline,theLocation,status,isImportant)
  constructor(id, title, duration, deadline, place, status, importance) {
    this.id = id;
    this.title = title;
    this.duration = duration;
    this.deadline = deadline;
    this.place = place;
    this.status = status;
    this.importance = importance;

    this.name = "kyle";
  }

  //    meow(){
  //         console.log("i meowing");
  //     };
}
